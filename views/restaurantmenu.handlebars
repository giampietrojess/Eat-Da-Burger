<div class="row">
  <div class="col s3"></div>
  <div class="col s6">
    <ul class="card center-align red darken-2">
      <h5>Burgers</h5>
        {{#each itm}}
          {{#if this.entree}}
              <p>
                {{this.id}}. {{this.item}}
              </p>
          {{/if}}
        {{/each}}
    </ul>
  </div>
</div>

